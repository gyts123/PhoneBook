{% extends 'base.html.twig' %}

{% block title %}PhoneBookEntry{% endblock %}

{% block body %}
    <h1>PhoneBookEntry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phone_book_entry.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ phone_book_entry.name }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ phone_book_entry.phoneNumber }}</td>
            </tr>
        </tbody>
    </table>



    <a href="{{ path('phone_book_entry_edit', {'id': phone_book_entry.id}) }}" class="btn btn-info">edit</a><br>

    {{ include('phone_book_entry/_delete_form.html.twig') }}
    <a href="{{ path('phone_book_entry_index') }}" class="btn btn-warning">back to list</a>
{% endblock %}
